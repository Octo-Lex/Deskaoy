# Next AI Reference

Local folder source for `C:\Next AI\ref`.

## Scope

- Browse and read any file under `C:\Next AI\ref`
- Search file contents with grep/find
- No write access in Explore mode

## Guidelines

- Use `read` to examine files
- Use `grep` and `find` to search
- Use `ls` to explore directories
